import React from 'react';
import {Box, FormControlLabel, FormGroup} from "@mui/material";

interface MyProps{}
interface MyState{
    switchBtn:boolean
}
class SwitchGroupComponent extends React.Component {
    render() {
        return (
            <Box>
                <FormGroup>
                    <FormControlLabel control={} label=''/>
                </FormGroup>
            </Box>
        );
    }
}

export default SwitchGroupComponent;